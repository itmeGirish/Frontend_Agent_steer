# CopilotKit Runtime URL (defaults to /api/copilotkit which proxies to LangGraph)
VITE_COPILOT_RUNTIME_URL=/api/copilotkit

# LangGraph Agent URL (used by Vite proxy)
# Default: http://localhost:8123
